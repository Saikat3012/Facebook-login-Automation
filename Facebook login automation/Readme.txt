As I had onlu used the chrome driver, it will only works over chrome browser, 
if you need to run it on another browser, then just download the driver of 
that browser, and rename it with the name "Chromedriver" and paste it on that folder.

Also Put your user id and Password in the code.